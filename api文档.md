
# 📷 Paddle OCR

1. **功能**：对输入的图像进行光学字符识别（OCR），返回图像中包含的文本内容。

2. **请求地址**：  
   `POST http://192.168.1.239:6788/recognize`

3. **输入参数**：

| 参数名   | 类型 | 必填 | 说明 |
|--------|------|-----|------|
| `images` | file | 是  | 待识别的图片文件，支持常见格式（如 `.jpg`, `.png`），MIME 类型需正确指定 |

#### 请求示例（curl）：
```bash
curl -X 'POST' \
  'http://192.168.1.239:6788/recognize' \
  -H 'accept: application/json' \
  -H 'Content-Type: multipart/form-data' \
  -F 'images=@test.jpg;type=image/jpeg'
```

4. **输出内容（JSON）**：

| 字段名        | 类型    | 说明 |
|--------------|--------|------|
| `success`    | bool   | 是否成功处理 |
| `timestamp`  | string | 处理完成的时间（ISO 8601 格式） |
| `total`      | int    | 上传的图片数量 |
| `results`    | list   | 每张图片的识别结果列表 |
| `saved_files`| list   | 服务端保存的结果文件路径（用于调试或可视化） |

##### `results` 中每个对象字段说明：

| 字段名     | 类型   | 说明 |
|-----------|--------|------|
| `filename`| string | 原始文件名 |
| `text`    | string | 识别出的文本内容（可能为空） |
| `score`   | float  | 识别置信度（当前版本可能固定为 0） |

##### 示例响应：
```json
{
  "success": true,
  "timestamp": "2025-12-11T07:31:24.661551",
  "total": 1,
  "results": [
    {
      "filename": "test.jpg",
      "text": "Example text from image",
      "score": 0.95
    }
  ],
  "saved_files": [
    {
      "image": "./output/20251211_073124_660927_rec_result.jpg"
    },
    {
      "json": "./output/20251211_073124_661455_result.json"
    }
  ]
}
```

---

# 🔍 YOLO 化合物位置检测

1. **功能**：对输入图像进行目标检测，定位其中的化学结构图（“compound”类别），返回边界框、类别和置信度。

2. **请求地址**：  
   `POST http://192.168.1.239:6789/detect`

3. **输入参数**：

| 参数名 | 类型 | 必填 | 说明 |
|-------|------|-----|------|
| `file` | file | 是  | 包含化学结构的图像文件（如 `.jpg`, `.png`） |

#### 请求示例（curl）：
```bash
curl -X 'POST' \
  'http://192.168.1.239:6789/detect' \
  -H 'accept: application/json' \
  -H 'Content-Type: multipart/form-data' \
  -F 'file=@test.jpg;type=image/jpeg'
```

4. **输出内容（JSON）**：

| 字段名             | 类型   | 说明 |
|--------------------|--------|------|
| `success`          | bool   | 是否成功 |
| `timestamp`        | string | 处理时间 |
| `original_image`   | string | 上传图像在服务器上的路径 |
| `visualized_image` | string | 带检测框的可视化图像 URL（可直接访问） |
| `detections`       | list   | 检测到的目标列表 |
| `total_objects`    | int    | 检测到的目标总数 |

##### `detections` 中每个对象字段说明：

| 字段名        | 类型    | 说明 |
|--------------|--------|------|
| `class_id`   | int    | 类别 ID（0 表示 compound） |
| `class_name` | string | 类别名称（如 `"compound"`） |
| `confidence` | float  | 检测置信度（0~1） |
| `bbox`       | list[float] | 边界框 `[x1, y1, x2, y2]`，左上角和右下角坐标（像素） |

##### 示例响应：
```json
{
  "success": true,
  "timestamp": "2025-12-11T07:35:23.592364",
  "original_image": "uploaded/20251211_073522_998079_original.jpg",
  "visualized_image": "http://192.168.1.239:6789/image/20251211_073522_998079_detected.jpg",
  "detections": [
    {
      "class_id": 0,
      "class_name": "compound",
      "confidence": 0.9267,
      "bbox": [468.88, 36.93, 615.23, 124.12]
    }
  ],
  "total_objects": 1
}
```

> 💡 提示：可通过 `visualized_image` URL 直接查看带框标注的图像。

---

# 🧬 化合物提取（PDF）

1. **功能**：从 PDF 文档中自动提取所有化学结构图及其关联信息（如 SMILES、名称、位置等），支持多页、多图、图文关联。

2. **请求地址**：  
   `POST http://192.168.1.239:6790/compound/`

3. **输入参数**：

| 参数名      | 类型 | 必填 | 说明 |
|------------|------|-----|------|
| `pdf_file` | file | 是  | 待处理的 PDF 文件（MIME 类型：`application/pdf`） |

#### 请求示例（curl）：
```bash
curl -X 'POST' \
  'http://192.168.1.239:6790/compound/' \
  -H 'accept: application/json' \
  -H 'Content-Type: multipart/form-data' \
  -F 'pdf_file=@CN110551144B.pdf;type=application/pdf'
```

4. **输出内容（JSON）**：

| 字段名       | 类型   | 说明 |
|-------------|--------|------|
| `success`   | bool   | 是否成功 |
| `compounds` | list   | 提取到的化合物图像块列表 |

##### 每个 `compounds` 元素字段说明：

| 字段名            | 类型   | 说明 |
|------------------|--------|------|
| `type`           | string | 内容类型（目前为 `"image"`） |
| `img_path`       | string | 提取出的图像文件相对路径 |
| `image_caption`  | list   | 图注文本（暂为空） |
| `image_footnote` | list   | 脚注文本（暂为空） |
| `bbox`           | list[int] | 该图像在 PDF 页面中的位置 `[x1, y1, x2, y2]`（单位：像素） |
| `page_idx`       | int    | 所在页码（从 1 开始） |
| `detect`         | list   | 在该图像中进一步检测到的化合物或文本区域 |

##### `detect` 中每个对象字段说明：

| 字段名             | 类型    | 说明 |
|--------------------|--------|------|
| `class_id`         | int    | 类别 ID（0=compound, 5=text） |
| `class_name`       | string | `"compound"` 或 `"text"` |
| `confidence`       | float  | 检测置信度 |
| `bbox`             | list[float] | 在图像内的坐标 |
| `name`             | string | 化合物标识（如 `"M-1"`） |
| `smiles`           | object/string | 若为化合物，包含 `{ "smiles": "..." }`；若为文本则为空字符串 |
| `crop_path`        | string | 裁剪后的子图路径 |
| `image_resolution` | list[int] | 原始图像分辨率 `[width, height]` |
| `original_image`   | string | 原始提取图像的绝对路径 |
| `bbox_id`          | int    | 检测框唯一 ID |
| `visualized_image` | string | 可视化结果图路径 |

##### 示例响应（简化）：
```json
{
  "success": true,
  "compounds": [
    {
      "type": "image",
      "img_path": "images/d5b6697779342ffd591e2ae3663ce8055b2d1ea86fcd95eb20530ae2285645d7.jpg",
      "bbox": [139, 121, 848, 290],
      "page_idx": 1,
      "detect": [
        {
          "class_id": 0,
          "class_name": "compound",
          "confidence": 0.9347,
          "bbox": [645.12, 120.28, 1154.31, 389.79],
          "name": "M-3",
          "smiles": { "smiles": "CC1(C)...[Li].[Mg]" },
          "crop_path": "./output/compound/segments/cmp_..._1_.jpg",
          "image_resolution": [1172, 397],
          "bbox_id": 1
        },
        {
          "class_id": 5,
          "class_name": "text",
          "confidence": 0.6477,
          "bbox": [787.02, 344.67, 829.41, 368.49],
          "name": "M-3",
          "smiles": "",
          "crop_path": "./output/compound/segments/cmp_..._4_.jpg",
          "bbox_id": 4
        }
      ]
    }
  ]
}
```

## 🧪 反应物提取

### 1. 功能描述  
从上传的 PDF 文档中自动识别并提取其中包含的**化学反应信息**，包括反应物（Reactants）、产物（Target）、溶剂（Solvents）、实验步骤（Experiments）、收率（Yields）等关键字段，并以结构化 JSON 格式返回。

该功能适用于专利、论文、实验报告等含有化学合成路径的 PDF 文件，支持多页、多反应提取。

---

### 2. 请求地址  
```
POST http://192.168.1.239:6790/reaction/
```

---

### 3. 输入参数  

| 参数名     | 类型   | 必填 | 说明 |
|------------|--------|------|------|
| `pdf_file` | file   | 是   | 待处理的 PDF 文件，MIME 类型为 `application/pdf` |

#### 示例请求（curl）：
```bash
curl -X 'POST' \
  'http://192.168.1.239:6790/reaction/' \
  -H 'accept: application/json' \
  -H 'Content-Type: multipart/form-data' \
  -F 'pdf_file=@CN110551144B.pdf;type=application/pdf'
```

---

### 4. 输出参数（JSON）

| 字段名        | 类型   | 说明 |
|---------------|--------|------|
| `success`     | bool   | 是否成功处理 |
| `reactions`   | list   | 提取到的反应列表，每个元素是一个反应对象 |

#### 每个 `reaction` 对象包含以下字段：

| 字段名         | 类型   | 说明 |
|----------------|--------|------|
| `reactants`    | string | SMILES 表示的完整反应式（格式：`A.B>>C`） |
| `target`       | string | 主要目标产物的 SMILES |
| `materials`    | string | 所有起始原料（逗号分隔的 SMILES） |
| `solvents`     | string | 使用的溶剂名称（如 "NMP", "DMF" 等） |
| `experiments`  | string | 对应的实验操作描述文本 |
| `page`         | int    | 该反应在 PDF 中出现的页码（从 1 开始） |
| `bbox`         | list[int] | 反应在页面中的边界框坐标 `[x1, y1, x2, y2]`（单位：像素，基于原始图像分辨率） |
| `yields`       | string | 收率信息（如 `"M-3: 65%"`） |
| `mapped_rxn`   | string | 原子映射后的反应 SMILES（用于机理分析或可视化） |
| `confidence`   | float  | 该反应提取结果的置信度（0~1），值越高越可靠 |

#### 示例响应：
```json
{
  "success": true,
  "reactions": [
    {
      "reactants": "C1=C(C=CC(=C1)O)[C@H](C(=O)O)NP=S.CC1(C)[C@H](C(=O)O)N2C(=O)[C@H]([C@H]2S1)N>>CC1(C)[C@H](C(=O)O)N2C(=O)[C@H]([C@H]2S1)NC(=O)[C@@H](C3=CC=C(C=C3)O)NP=O.[Li].[Mg]",
      "target": "CC1(C)[C@H](C(=O)O)N2C(=O)[C@H]([C@H]2S1)NC(=O)[C@@H](C3=CC=C(C=C3)O)NP=O.[Li].[Mg]",
      "materials": "C1=C(C=CC(=C1)O)[C@H](C(=O)O)NP=S,CC1(C)[C@H](C(=O)O)N2C(=O)[C@H]([C@H]2S1)N",
      "solvents": "NMP",
      "experiments": "称取5mmol化合物I-2溶解于50ml NMP中, 冰浴, 先后加入6mmol氯甲酸乙酯, 6mmol N,N-二异丙基乙胺, 保持在0摄氏度下搅拌20分钟。将5.5mmol化合物I-3和15ml碳酸氢钠溶液(约10%)的混合液, 在-60~-45摄氏度下迅速滴加至前述反应溶液中, 保持-15~0摄氏度搅拌1~2小时, 恢复至室温搅拌1小时。减压浓缩蒸除溶剂, 乙醚萃取, 水相酸化至pH=3, 乙酸乙酯提取, 饱和食盐水洗涤, 无水硫酸钠干燥, 过滤, 减压旋蒸, 硅胶柱层析纯化",
      "page": 1,
      "bbox": [139, 121, 848, 290],
      "yields": "M-3: 65%",
      "mapped_rxn": "[CH:23]1=[C:18]([C@H:17]([C:15](=[O:16])[OH:27])[NH:25][P:26]=S)[CH:19]=[CH:20][C:21]([OH:24])=[CH:22]1.[CH3:1][C:2]1([CH3:3])[C@H:4]([C:5](=[O:6])[OH:7])[N:8]2[C:9](=[O:10])[C@@H:11]([NH2:14])[C@H:12]2[S:13]1>>[CH3:1][C:2]1([CH3:3])[C@H:4]([C:5](=[O:6])[OH:7])[N:8]2[C:9](=[O:10])[C@@H:11]([NH:14][C:15](=[O:16])[C@@H:17]([C:18]3=[CH:19][CH:20]=[C:21]([OH:24])[CH:22]=[CH:23]3)[NH:25][P:26]=[O:27])[C@H:12]2[S:13]1.[Li:28].[Mg:29]",
      "confidence": 0.59038871689292
    }
  ]
}
```

> ⚠️ 注意：若 PDF 中无有效反应，`reactions` 将为空列表 `[]`，但 `success` 仍为 `true`。

---

## 🌐 中英文双栏翻译

### 1. 功能描述  
接收一个 PDF 文件，自动识别其内容（特别是学术/专利类文档），将每一页的**中文内容翻译成英文**（或反之），并生成一个新的 **左右双栏对照 PDF**：左栏为原文，右栏为译文。适用于中英科技文献的快速对照阅读。

---

### 2. 请求地址  
```
POST http://192.168.1.239:6791/translate-dual-and-merge
```

> ✅ 注意：端口为 `6791`，与前几个服务不同。

---

### 3. 输入参数  

| 参数名     | 类型   | 必填 | 说明 |
|------------|--------|------|------|
| `pdf_file` | file   | 是   | 待翻译的 PDF 文件，MIME 类型为 `application/pdf` |

#### 示例请求（curl）：
```bash
curl -X 'POST' \
  'http://192.168.1.239:6791/translate-dual-and-merge' \
  -H 'accept: application/json' \
  -H 'Content-Type: multipart/form-data' \
  -F 'pdf_file=@CN110551144B.pdf;type=application/pdf'
```

> ❗ 修正：原初稿中 curl 地址写错了（写成了 `/reaction/`），已更正为正确 endpoint。

---

### 4. 输出内容  

- **响应类型**：`application/pdf`
- **内容**：一个新生成的 PDF 文件，每页为**双栏布局**：
  - 左侧：原始文本（保留原始排版、公式、图片位置）
  - 右侧：对应段落的机器翻译结果（英文或中文）
- 文件通过 HTTP 响应体直接返回（非 JSON）

#### 使用建议：
- 客户端应以二进制方式接收响应，并保存为 `.pdf` 文件。
- 不返回 JSON，因此 `accept: application/json` 实际可省略或改为 `*/*`。

#### 示例（Python requests）：
```python
import requests

with open("input.pdf", "rb") as f:
    files = {"pdf_file": ("input.pdf", f, "application/pdf")}
    resp = requests.post(
        "http://192.168.1.239:6791/translate-dual-and-merge",
        files=files
    )

with open("output_dual_translated.pdf", "wb") as out:
    out.write(resp.content)
```